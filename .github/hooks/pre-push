#!/bin/bash
# 🛡️ Git Guard: MarioGeo Pre-push
echo "🚀 Verificando integridad antes del push..."

# Ejecutamos el test del Makefile
make test

if [ $? -ne 0 ]; then
    echo "❌ [ERROR] Los tests fallaron. El push ha sido bloqueado."
    echo "Corrige el código antes de intentar subirlo."
    exit 1
fi

echo "✅ Tests pasados. Iniciando subida a GitHub..."
exit 0